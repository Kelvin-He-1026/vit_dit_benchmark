# Main benchmark harness: vit_benchmark.py, dit_benchmark_cpuOffload.py,
# run_multisocket.py, consolidate_results.py
#
# Pinned to the versions in cv_env that produced the results in output/.
# Python 3.12.
#
# NOTE: this file is NOT compatible with vllm_dit_vit_benchmark.py, which needs
# transformers 5.x / huggingface_hub 1.x. Use requirements-vllm.txt in a
# SEPARATE venv for that script.
#
# Torch here is a CUDA 13.0 build. For a different CUDA/CPU build, install torch
# first from the matching index, e.g.:
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cu130
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cpu

torch==2.13.0
torchvision==0.28.0
# W8A8 quantisation kernels behind --quant in vit_benchmark.py and
# server_vit_benchmark.py. Optional: both scripts run without it and only
# raise if --quant asks for something other than none. Install with
# --no-deps to keep the torch pin above from being resolved again.
#
# Importing it prints two deprecation warnings against torch 2.13:
#   <enum 'KernelPreference'> ... register_constant() on Enum subclasses is
#   deprecated and will be an error in a future release
# They come from torchao's register_as_pytree_constant (torchao/utils.py),
# which registers enums that torch now handles natively, and they fire at
# import before any benchmark code runs. Harmless at these pins and not
# suppressed on purpose: torch says it will make this an error, so the day
# these warnings turn into an ImportError is the day torchao needs upgrading.
# No released torchao fixes it yet (0.18.0 is the newest as of 2026-09-15).
# Check after any torch bump with: python -c "import torchao"
torchao==0.18.0

transformers==4.57.6
diffusers==0.39.0
accelerate==1.14.0
datasets==5.0.1

huggingface_hub==0.36.2
hf-xet==1.6.0
safetensors==0.8.0

# T5 text encoders used by PixArt-Sigma / SD3.5 / Sana
sentencepiece==0.2.2
protobuf==7.36.0
ftfy==6.3.1

pillow==12.3.0
numpy==2.5.2

# Resource monitoring for server_vit_benchmark.py. Both are optional: the
# benchmark runs without them and simply reports the metrics it can still get.
#   psutil        host CPU / RSS / thread count
#   nvidia-ml-py  NVML device counters (utilisation, power, clocks, memory).
#                 This is NVIDIA's own binding and imports as `pynvml`; the
#                 separate PyPI package literally named `pynvml` is a different,
#                 deprecated project - do not install that one.
psutil==7.2.2
nvidia-ml-py==13.610.43
